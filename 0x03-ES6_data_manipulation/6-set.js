/**
 * Creates a new Set from the elements in the given array.
 *
 * @param {Array} array - The array to create the Set from
 * @return {Set} A new Set containing the elements from the array
 */
export default function setFromArray(array) {
  return new Set(array);
}
